# Отчёт по системному (E2E) тестированию

**Проект:** XKCD Comics Aggregator (xs)

**Дата:** 09.04.2026

**Команда:** 6 человек

---

## 1. Описание выполненной работы

### 1.1 Объект тестирования

Системное тестирование проверяет готовый продукт по бизнес-требованиям в полностью рабочем окружении. Тестируются два компонента:

- **REST API** (`cmd/xkcd`, порт 8080) — реальный бинарник с реальной SQLite БД и настроенным пользователем
- **Веб-фронтенд** (`cmd/web`, порт 8090) — реальный веб-сервер, проксирующий запросы к API

### 1.2 Виды системных тестов

| Вид | Кол-во | Описание |
|-----|--------|----------|
| API-Chain E2E (Python + requests) | 3 | Многошаговые цепочки HTTP-запросов к реальному API-бинарнику |
| Browser E2E (Playwright) | 12 | Сценарии взаимодействия пользователя с веб-интерфейсом |
| **Итого** | **15** | |

### 1.3 Используемые инструменты

| Инструмент | Назначение |
|------------|------------|
| Python 3 + `requests` | HTTP-клиент для API-chain тестов |
| Playwright (sync API) + pytest | Браузерное E2E-тестирование веб-фронтенда |
| `chromium` | Браузер для Playwright |
| GitHub Actions | Сервер непрерывной интеграции |
| `make e2e` | Запуск API-chain тестов (сборка + старт бинарника + тесты + остановка) |
| `make e2e-playwright` | Установка зависимостей + запуск Playwright тестов |

### 1.4 Тестовое окружение

**API-Chain:** реальный бинарник `build/xkcd` запускается на случайном порту через `make e2e`. БД предварительно заполняется командой `/api/update`. `API_BASE_URL = http://localhost:{PORT}`.

**Browser E2E:** веб-сервер `build/web-server` запущен на порту 8090, проксирует к API. `BASE_URL = http://localhost:8090`. Пользователь `bob/bob` должен существовать в БД.

### 1.5 Классификация тестов

#### По уровню изоляции

Системные (E2E) тесты обеспечивают **минимальную изоляцию** — проверяется готовый продукт в условиях, максимально приближённых к production:

| Компонент | API-Chain тесты | Browser E2E тесты |
|-----------|-----------------|-------------------|
| **БД** | Реальная SQLite | Реальная SQLite |
| **REST API** | Реальный бинарник | Реальный бинарник (через прокси) |
| **Веб-сервер** | Не используется | Реальный бинарник |
| **XKCD API** | Реальный (xkcd.com) | Реальный |
| **Браузер** | — | Chromium (Playwright) |

#### По подходу к тестированию

| Подход | Описание | Применение |
|--------|----------|------------|
| **Чёрный ящик (black-box)** | Тестировщик взаимодействует с системой только через публичный интерфейс | API-Chain: HTTP-запросы к реальному API; Browser: действия пользователя в браузере (клики, заполнение форм) |

#### По назначению теста

| Назначение | API-Chain | Playwright | Итого |
|------------|-----------|------------|-------|
| Штатный сценарий (позитивный) | 2 | 8 | **10** |
| Путь ошибки (негативный) | 1 | 3 | **4** |
| Граничный | 0 | 1 | **1** |
| **Итого** | **3** | **12** | **15** |

#### По объекту тестирования

| Категория | Файл | Тесты | Предусловия |
|-----------|------|-------|-------------|
| Обновление индекса (API) | `test/e2e/update.py` | 2 | Бинарник запущен; пользователь `bob/bob` с ролью admin существует |
| Поиск + комикс (API) | `test/e2e/pics.py` | 1 | Бинарник запущен; индекс заполнен; пользователь `bob/bob` существует |
| Авторизация (браузер) | `test/e2e/playwright/test_web.py` | 2 | Веб-сервер запущен; `bob/bob` в БД |
| Поиск (браузер) | `test/e2e/playwright/test_web.py` | 3 | Веб-сервер запущен; индекс заполнен |
| Избранное (браузер) | `test/e2e/playwright/test_web.py` | 5 | Веб-сервер запущен; `bob/bob` в БД; индекс заполнен |
| Навигация (браузер) | `test/e2e/playwright/test_web.py` | 2 | Веб-сервер запущен |

### 1.6 Непрерывная интеграция

```
make build → make e2e (API-chain) → make e2e-playwright (Browser)
```

API-chain тесты в `make e2e`: сборка бинарника → запуск на фоне → `sleep 10s` → Python-скрипты → kill. Playwright тесты: установка зависимостей → `playwright install chromium` → `pytest test/e2e/playwright/ -v`.

---

## 2. Тест-план

### 2.1 API-Chain сценарии (3)

| № | Сценарий | Тип | Предусловия | Эндпоинты |
|---|----------|-----|-------------|-----------|
| 1 | Администратор получает JWT и запускает обновление индекса | Позитивный | Бинарник запущен; `bob/bob` с ролью admin | `POST /api/login` → `POST /api/update` |
| 2 | Попытка обновления индекса без токена авторизации | Негативный | Бинарник запущен | `POST /api/update` (без заголовка) |
| 3 | Пользователь ищет комиксы и получает один из них по ID | Позитивный | Индекс заполнен; `bob/bob` существует | `POST /api/login` → `GET /api/pics` → `GET /api/comic/{id}` |

### 2.2 Browser E2E сценарии (12)

| № | Сценарий | Тип | Предусловия |
|---|----------|-----|-------------|
| 4 | Вход с неверным паролем → остаётся на /login | Негативный | Веб-сервер запущен; пользователь не авторизован |
| 5 | Неверный пароль → верный пароль → поиск комикса | Негативный + Позитивный | `bob/bob` существует в БД; индекс заполнен; пользователь не авторизован |
| 6 | Анонимный пользователь ищет комикс — нет кнопок избранного | Позитивный | Индекс заполнен; пользователь не авторизован |
| 7 | Несуществующий запрос, затем реальный — получены результаты | Граничный | Индекс заполнен; пользователь не авторизован |
| 8 | Авторизованный пользователь видит кнопки избранного, анонимный — нет | Позитивный | `bob/bob` существует в БД; индекс заполнен |
| 9 | Войти, убедиться в пустом /favorites, найти и добавить комикс → он на /favorites | Позитивный | `bob/bob` в БД; индекс заполнен; избранное пустое |
| 10 | Добавить два комикса, удалить один — на /favorites остался только нужный | Позитивный | `bob/bob` в БД; индекс заполнен; избранное пустое |
| 11 | Добавить комикс в избранное, удалить его — список снова пустой | Позитивный | `bob/bob` в БД; индекс заполнен; избранное пустое |
| 12 | Добавить комикс, выполнить новый поиск — избранное сохранилось | Позитивный | `bob/bob` в БД; индекс заполнен; избранное пустое |
| 13 | Анонимный пользователь открывает /favorites — нет чужих данных | Негативный | Пользователь не авторизован; cookie Favorites отсутствует |
| 14 | Войти, добавить комикс, перейти на главную через navbar → избранное сохранилось | Позитивный | `bob/bob` в БД; индекс заполнен |
| 15 | Поисковый запрос отражается в URL/поле после submit, следующий поиск обновляет его | Позитивный | Пользователь не авторизован |

### 2.3 Описание сценариев

---

#### РАЗДЕЛ A. Обновление индекса (API)

---

**Сценарий 1: Администратор получает JWT и запускает обновление индекса**

- Предусловия: бинарник `build/xkcd` запущен; пользователь `bob/bob` с правами admin существует в БД
- Шаг 1: `POST /api/login {"login":"bob","password":"bob"}` — пользователь входит в систему
- Шаг 2: извлечь токен из заголовка `Authorization` ответа — получен JWT
- Шаг 3: `POST /api/update` с заголовком `Authorization: <token>` — запрос на обновление индекса
- Ожидаемый результат: оба запроса возвращают HTTP 200; индекс обновлён

---

**Сценарий 2: Попытка обновления индекса без токена авторизации**

- Предусловия: бинарник `build/xkcd` запущен; токен не получался
- Шаг 1: `POST /api/update` без заголовка `Authorization` — попытка вызвать защищённый endpoint
- Ожидаемый результат: HTTP 401 Unauthorized; индекс не изменился

---

#### РАЗДЕЛ B. Поиск и получение комикса (API)

---

**Сценарий 3: Пользователь ищет комиксы и получает один по ID**

- Предусловия: бинарник запущен; индекс заполнен; пользователь `bob/bob` существует; запрос `apple,doctor` возвращает комикс с `img = "https://imgs.xkcd.com/comics/an_apple_a_day.png"`
- Шаг 1: `POST /api/login {"login":"bob","password":"bob"}` — пользователь входит в систему, получает JWT
- Шаг 2: `GET /api/pics?search=apple,doctor` с токеном — пользователь выполняет поиск, проверяет наличие ожидаемого URL в результатах
- Шаг 3: `GET /api/comic/{id}` с ID первого результата — пользователь получает конкретный комикс по ID, проверяет совпадение поля `id`
- Ожидаемый результат: поиск находит ожидаемый комикс; `/api/comic/{id}` возвращает тот же ID

---

#### РАЗДЕЛ C. Авторизация (Browser)

---

**Сценарий 4: Вход с неверным паролем → остаётся на /login**

- Предусловия: веб-сервер запущен; пользователь не авторизован
- Шаг 1: открыть `/login`
- Шаг 2: ввести логин `"bob"`, пароль `"wrongpassword"`
- Шаг 3: нажать Enter — отправить форму
- Шаг 4: дождаться загрузки
- Ожидаемый результат: URL содержит `/login`; пользователь не перенаправлен
- Постусловие: cookie Authorization не установлена

---

**Сценарий 5: Неверный пароль, затем верный → редирект на главную + успешный поиск**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; веб-сервер запущен; пользователь не авторизован
- Шаг 1: открыть `/login`
- Шаг 2: ввести логин `"bob"`, пароль `"wrongpassword"` → нажать Enter
- Шаг 3: убедиться что остались на `/login` — первая попытка не удалась
- Шаг 4: ввести логин `"bob"`, пароль `"bob"` → нажать Enter
- Шаг 5: дождаться редиректа на главную страницу
- Шаг 6: ввести `"apple"` в поле поиска → нажать Enter
- Шаг 7: дождаться результатов
- Ожидаемый результат: после второй попытки URL не содержит `/login`; на главной отображаются результаты поиска с комиксами
- Постусловие: пользователь авторизован; cookie Authorization установлена

---

#### РАЗДЕЛ D. Поиск (Browser)

---

**Сценарий 6: Анонимный пользователь открывает главную, ищет комикс — кнопок избранного нет**

- Предусловия: индекс заполнен; веб-сервер запущен; пользователь не авторизован
- Шаг 1: открыть `/`
- Шаг 2: убедиться что присутствует форма с текстовым полем
- Шаг 3: ввести `"apple"` → нажать Enter
- Шаг 4: дождаться загрузки результатов
- Ожидаемый результат: страница содержит элементы комиксов; кнопок избранного (`.favorite-star`) не отображается — анонимный пользователь не может добавлять в избранное
- Постусловие: состояние системы не изменилось

---

**Сценарий 7: Несуществующий поисковый запрос, затем реальный — получены результаты**

- Предусловия: индекс заполнен; веб-сервер запущен; пользователь не авторизован
- Шаг 1: открыть `/`
- Шаг 2: ввести `"xyzzy_no_such_comic_ever"` → нажать Enter
- Шаг 3: убедиться что список пуст или отображается сообщение об отсутствии результатов; нет 5xx ошибки
- Шаг 4: ввести `"apple"` → нажать Enter
- Шаг 5: дождаться загрузки результатов
- Ожидаемый результат: второй поиск возвращает комиксы; система корректно обрабатывает оба запроса
- Постусловие: состояние системы не изменилось

---

**Сценарий 8: Авторизованный пользователь видит кнопки избранного, анонимный — нет**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; веб-сервер запущен
- Шаг 1: открыть `/`, ввести `"apple"` → нажать Enter
- Шаг 2: убедиться что кнопок избранного нет — пользователь анонимный
- Шаг 3: открыть `/login`, ввести `bob/bob` → нажать Enter, дождаться редиректа
- Шаг 4: перейти на `/`, ввести `"apple"` → нажать Enter
- Шаг 5: дождаться результатов
- Ожидаемый результат: после входа у каждого комикса в результатах видна кнопка избранного (`.favorite-star`)
- Постусловие: пользователь авторизован

---

#### РАЗДЕЛ E. Избранное (Browser)

---

**Сценарий 9: Войти, убедиться в пустом /favorites, найти комикс, добавить → он появился на /favorites**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; cookie Favorites отсутствует; веб-сервер запущен
- Шаг 1: открыть `/login`, ввести `bob/bob` → нажать Enter
- Шаг 2: открыть `/favorites` — убедиться что список пуст
- Шаг 3: перейти на `/`, ввести `"apple"` → нажать Enter
- Шаг 4: запомнить title первого комикса в результатах
- Шаг 5: нажать кнопку избранного у первого комикса
- Шаг 6: открыть `/favorites`
- Ожидаемый результат: на `/favorites` отображается именно тот комикс, title которого запомнили на шаге 4
- Постусловие: cookie Favorites содержит ID добавленного комикса

---

**Сценарий 10: Добавить два комикса в избранное, удалить один — на /favorites остался только нужный**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; cookie Favorites отсутствует; веб-сервер запущен
- Шаг 1: открыть `/login`, ввести `bob/bob` → нажать Enter
- Шаг 2: перейти на `/`, ввести `"apple"` → нажать Enter
- Шаг 3: запомнить title первого и второго комикса в результатах
- Шаг 4: нажать кнопку избранного у первого комикса
- Шаг 5: нажать кнопку избранного у второго комикса
- Шаг 6: открыть `/favorites` — убедиться что оба комикса присутствуют
- Шаг 7: нажать кнопку избранного у первого комикса (удаление)
- Шаг 8: открыть `/favorites`
- Ожидаемый результат: на `/favorites` присутствует только второй комикс; первый отсутствует
- Постусловие: cookie Favorites содержит только ID второго комикса

---

**Сценарий 11: Добавить комикс в избранное, затем убрать — список снова пустой**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; cookie Favorites отсутствует; веб-сервер запущен
- Шаг 1: открыть `/login`, ввести `bob/bob` → нажать Enter
- Шаг 2: перейти на `/`, ввести `"apple"` → нажать Enter
- Шаг 3: нажать кнопку избранного у первого комикса (добавление)
- Шаг 4: открыть `/favorites` — убедиться что комикс присутствует
- Шаг 5: нажать кнопку избранного у того же комикса (удаление)
- Шаг 6: открыть `/favorites`
- Ожидаемый результат: список избранного пуст
- Постусловие: cookie Favorites содержит пустой массив

---

**Сценарий 12: Добавить комикс, выполнить новый поиск — избранное сохранилось**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; cookie Favorites отсутствует; веб-сервер запущен
- Шаг 1: открыть `/login`, ввести `bob/bob` → нажать Enter
- Шаг 2: перейти на `/`, ввести `"apple"` → нажать Enter
- Шаг 3: нажать кнопку избранного у первого комикса (добавление)
- Шаг 4: ввести `"linux"` в поле поиска → нажать Enter
- Шаг 5: дождаться новых результатов
- Шаг 6: открыть `/favorites`
- Ожидаемый результат: на `/favorites` комикс из шага 3 всё ещё присутствует; новый поиск не сбросил избранное
- Постусловие: cookie Favorites сохранена между поисками

---

**Сценарий 13: Анонимный пользователь открывает /favorites — пустое состояние, не чужие данные**

- Предусловия: веб-сервер запущен; пользователь не авторизован; cookie Favorites отсутствует
- Шаг 1: открыть `/favorites` напрямую без авторизации
- Шаг 2: дождаться загрузки страницы
- Ожидаемый результат: статус < 500; список пуст или редирект на `/login`; никаких чужих данных не отображается
- Постусловие: состояние системы не изменилось

---

#### РАЗДЕЛ F. Навигация (Browser)

---

**Сценарий 14: Войти, добавить комикс, перейти на главную через navbar → избранное сохранилось**

- Предусловия: пользователь `bob/bob` существует в БД; индекс заполнен; веб-сервер запущен
- Шаг 1: открыть `/login`, ввести `bob/bob` → нажать Enter
- Шаг 2: ввести `"apple"` → нажать Enter; добавить первый комикс в избранное
- Шаг 3: открыть `/favorites` — убедиться что комикс есть
- Шаг 4: кликнуть ссылку Home/логотип в navbar
- Шаг 5: дождаться загрузки главной страницы
- Шаг 6: открыть `/favorites` снова
- Ожидаемый результат: после навигации через navbar комикс всё ещё присутствует в избранном
- Постусловие: URL равен `BASE_URL/`; cookie Favorites не сброшена

---

**Сценарий 15: Поисковый запрос отражается в URL после submit, следующий поиск обновляет его**

- Предусловия: веб-сервер запущен; пользователь не авторизован
- Шаг 1: открыть `/`
- Шаг 2: ввести `"apple"` → нажать Enter
- Шаг 3: проверить что `"apple"` присутствует в URL или в поле ввода
- Шаг 4: ввести `"linux"` → нажать Enter
- Шаг 5: проверить что URL или поле ввода обновились на `"linux"`
- Ожидаемый результат: каждый поиск отражается в состоянии страницы; пользователь видит какой запрос сейчас активен
- Постусловие: состояние системы не изменилось
